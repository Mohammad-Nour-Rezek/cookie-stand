Seattle = {
    min: 23,
    max: 65,
    avg: 6.3,
    customerPerHoure() {
        return Math.floor(Math.random() * 10);
    }
};

Tokyo = {
    min: 3,
    max: 24,
    avg: 1.2,
    customerPerHoure() {
        return Math.floor(Math.random() * 10);
    }
};

Dubai = {
    min: 11,
    max: 38,
    avg: 3.7,
    customerPerHoure() {
        return Math.floor(Math.random() * 10);
    }
};

Paris = {
    min: 20,
    max: 38,
    avg: 2.3,
    customerPerHoure() {
        return Math.floor(Math.random() * 10);
    }
};

Lima = {
    min: 2,
    max: 16,
    avg: 4.6,
    customerPerHoure() {
        return Math.floor(Math.random() * 10);
    }
};